name: lanuage
description: A new Flutter project.

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  # Core packages
  camera: ^0.11.1
  permission_handler: ^12.0.0+1
  provider: ^6.0.4
  
  # UI packages
  google_fonts: ^6.2.1
  flutter_animate: ^4.1.1+1
  font_awesome_flutter: ^10.1.0
  lottie: ^3.3.1
  
  # ML and Translation packages
  google_mlkit_text_recognition: ^0.15.0
  google_mlkit_language_id: ^0.13.0
  translator: ^1.0.3+1
  
  # Utilities
  intl: ^0.20.2
  shared_preferences: ^2.0.15
  url_launcher: ^6.1.10
  go_router: ^15.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/
    - assets/jsons/